spring:
  application:
    name: blogPessoal
  datasource:
    url: jdbc:postgresql://localhost:5433/blogPessoal
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
